export struct MenuItemInfo {
    name: string,
    icon_name: string,
    icon: image,
    panel_name: string,
    is_submenu: bool,
}

export struct GCodeFileInfo {
    name: string,
    is_dir: bool,
    size: string,
    thumbnail: image,
}

export struct ConsoleLine {
    text: string,
    is_input: bool,
}
